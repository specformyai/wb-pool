.venv/
__pycache__/
*.pyc
data/
jscache/
.env
*.tmp
probe*.py
models_ok.json
rates.json

# agent 一次性诊断/改造脚本（本地临时，不入库）
patch_*.py
fix_*.py
inspect_*.py
diag_*.py
check_*.py
dump_*.py
drop_*.py
batch_probe.py
find_full_phone.py
list_all_accounts.py
pool_analysis.py
/test_upstream_sync.py
/main.py
/upstream.py
/upstream_sync.py
/uoomsg.py
/app.js
/index.html
*.tar.gz
ab_test_*.py
control_test.py
e2e_*.py
retest_*.py
scan_*.py
verify_*.py
probe_*.py

# 上面那批 verify_*/probe_*/check_* 通配是为了挡掉根目录的一次性诊断脚本。
# 但 tests/ 下的验证脚本是项目正式资产（CI 要跑），必须显式放行 ——
# 否则它们会被静默忽略，本地跑得通、推上去别人却拿不到。
!tests/verify_*.py
!scripts/verify_*.py

# 补丁脚本留下的时间戳备份。改代码前备份是对的，但它们是本地回滚用的，
# 不该进版本库（git 本身就是版本库）。
*.bak
*.bak-*

# 账号历史对话导出（含真实对话正文与手机号，绝不入库）
usage_dump/
dead_dump*/
history_*.txt

# 本轮一次性运维脚本（不是可复用工具）
compare_accounts.py
refresh_all.py
restore_disabled.py

# 被取代的产物：余额快照时间序列（本意是区分「真扣费」与「包到期」，
# 现已由 get-user-request-usage 逐笔流水 + 包级到期感知直接解决），从未运行过
scripts/balance_snapshot.py
